@model IEnumerable<Alumni_Management_System.Models.OrganizationType>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OrganizationName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OrganizationName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OrganizationTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OrganizationTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OrganizationTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
