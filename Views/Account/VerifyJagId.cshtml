@model Alumni_Management_System.Models.ViewModels.VerifyJagIdViewModel

@{
    ViewData["Title"] = "Verify JAG ID";
}
@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show shadow-sm border-0 mb-4" role="alert">
        <i class="bi bi-exclamation-octagon-fill me-2"></i>
        @TempData["ErrorMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible fade show shadow-sm border-0 mb-4" role="alert">
        <i class="bi bi-check-circle-fill me-2"></i>
        @TempData["SuccessMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h3 class="mb-0"><i class="bi bi-person-check"></i> Alumni Registration - Step 1</h3>
                </div>
                <div class="card-body">
                    <h5 class="card-title">Verify Your JAG ID</h5>
                    <p class="text-info">
                        Please enter your JAG ID and Last Name to begin the registration process. And last name must match the one used at the time of graduation.
                    </p>

                    <form asp-action="VerifyJagId" method="post">
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>

                        <div class="mb-3">
                            <label asp-for="JagId" class="form-label"></label>
                            <input asp-for="JagId" class="form-control form-control-lg" autofocus  />
                            <span asp-validation-for="JagId" class="text-danger"></span>
                            <div class="form-text">
                                <i class="bi bi-info-circle"></i> JAG ID must start with 'J' followed by numbers (e.g., J0012345)
                            </div>
                        </div>
                        <div class="mb-3">
                            <label asp-for="LastName" class="form-label"></label>
                            <input asp-for="LastName" class="form-control form-control-lg" />
                            <span asp-validation-for="LastName" class="text-danger"></span>
                            <div class="form-text">
                                <i class="bi bi-info-circle"></i> Please enter your last name while graduating.
                            </div>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-primary btn-lg">
                                <i class="bi bi-arrow-right-circle"></i> Verify Credentials
                            </button>
                        </div>
                    </form>

                    <hr class="my-4">

                    <div class="text-center">
                        <p class="mb-2">Already have an account?</p>
                        <a asp-area="Identity" asp-page="/Account/Login" class="btn btn-outline-secondary">
                            <i class="bi bi-box-arrow-in-right"></i> Sign In
                        </a>
                    </div>

                    <div class="alert alert-info mt-3" role="alert">
                        <h6 class="alert-heading"><i class="bi bi-question-circle"></i> Need Help?</h6>
                        <p class="mb-0">
                            If your JAG ID is not found or you're experiencing issues, please contact the administrator for assistance.
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
